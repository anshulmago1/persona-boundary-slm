# GPU training deps. Install on the machine that has an NVIDIA GPU (Colab / Modal / RunPod).
# NOT needed for local data generation or eval orchestration.
#
# On a CUDA box, Unsloth gives ~2x faster / ~70% less VRAM QLoRA. If Unsloth is not
# available, train.py falls back to TRL + PEFT + bitsandbytes.
#
# Recommended install order on a fresh CUDA machine:
#   pip install "unsloth[cu121-torch240] @ git+https://github.com/unslothai/unsloth.git"
# or the plain stack below:

torch>=2.4.0
transformers>=4.44.0
datasets>=2.20.0
trl>=0.9.6
peft>=0.12.0
accelerate>=0.33.0
bitsandbytes>=0.43.0
# unsloth  # uncomment/install per the Unsloth instructions for your CUDA version
