# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-train/
venv/
.env

# Data artifacts (regenerated by the pipeline)
data/raw/*.jsonl
data/filtered/*.jsonl
data/eval/*.json
data/eval/*.jsonl
data/eval/*.csv
data/smoke/
!data/**/.gitkeep

# Model / training outputs
outputs/
checkpoints/
*.gguf
*.safetensors

# Notebook checkpoints
.ipynb_checkpoints/

# macOS
.DS_Store

# Assignment spec PDF — not ours to redistribute publicly
Train Your Own Small Learning Model.pdf
data/full_run.log

# Row A grader: raw College Board material is copyrighted — keep local, never publish.
# Regenerable via `python -m src.rowa.scrape` + `parse_pdf` + `build_gold`.
data/rowa/pdfs/
data/rowa/*_raw.jsonl
data/rowa/page_images/
data/rowa/gold.jsonl
data/rowa/gold_all.jsonl
# Result files embed verbatim thesis text; keep the metrics-only CSV (committed).
data/rowa/eval_results.json
data/rowa/litmus_gate.json
data/*.log
# Publishable (fully synthetic / metrics only): train*.jsonl, dev_v2.jsonl, dpo*.jsonl,
# eval_results.csv, DATASET_CARD.md — these may be committed.
